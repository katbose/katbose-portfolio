<!-- Cover placeholder for posts with no `coverImage`.
     No background fill and no outer frame on purpose: the card that hosts this
     already draws a border, and the cover sits flush against it, so an inset
     frame here reads as a doubled border. Just a centred, muted image glyph. -->
<svg xmlns="http://www.w3.org/2000/svg" width="640" height="360" viewBox="0 0 640 360" fill="none">
  <g stroke="#a1a1aa" stroke-width="4" stroke-linecap="round" stroke-linejoin="round">
    <rect x="235" y="116" width="170" height="128" rx="14" />
    <path d="M248 232l52-52 32 32 24-24 36 44" />
  </g>
  <circle cx="275" cy="152" r="13" fill="#a1a1aa" />
</svg>
