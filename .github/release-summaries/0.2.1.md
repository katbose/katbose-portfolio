## Highlights

This patch strengthens security checks and makes browser test failures easier to spot.

- Server-rendering tests now parse HTML with `DOMParser`, addressing the flagged regular expressions.
- CodeQL covers JavaScript/TypeScript and GitHub Actions with extended security queries.
- Browser tests no longer hide console errors based on hostname.
