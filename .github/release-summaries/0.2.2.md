## Highlights

This release improves release automation and makes the changelog easier to scan.

- Combined release PRs use the correct publication identity and retain the `chore(release): vX.Y.Z` title.
- Published notes keep every categorized change visible, with contributor credits and a full comparison link.
- Regression checks catch configuration drift, and the workflow reports merged release PRs that remain unpublished.
